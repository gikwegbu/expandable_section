name: expandable_section
description: A Flutter package that enables you to expand and contract a section on your display screen.
version: 0.0.3
homepage: https://github.com/gikwegbu/expandable_section

environment:
  sdk: ">=2.16.2 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  
flutter:
